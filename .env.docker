# ==================================
# Emby Panel Control - Docker Configuration
# ==================================

# User / Group Identifiers
PUID=1000
PGID=1000
TZ=Etc/UTC

# Emby Server Configuration
EMBY_SERVER_URL=http://localhost:8096
EMBY_API_KEY=your_api_key_here

# Panel Configuration
CRON_SECRET=your_secret_here
BASE_URL=http://localhost:44444

# Admin Credentials (optional - only used on first run)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# ==================================
# NOTES:
# ==================================
# 1. Get EMBY_API_KEY from: Emby Server > Dashboard > Advanced > API Keys
# 2. Generate CRON_SECRET with: openssl rand -hex 32
# 3. ADMIN credentials are only used when creating the database for the first time
# 4. Change localhost to your actual domain/IP in production
# 5. TZ options: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
