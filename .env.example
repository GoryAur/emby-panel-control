# ===========================================
# Variables de Entorno - Emby Panel Control
# ===========================================

# ============================================
# CONFIGURACIÓN OBLIGATORIA
# ============================================

# URL del servidor Emby principal
EMBY_SERVER_URL=http://localhost:8096

# API Key del servidor Emby (obtener desde Dashboard > Advanced > Security)
EMBY_API_KEY=tu_api_key_aqui

# Secret para proteger endpoint de cron /api/cron/disable-expired
# Genera uno seguro con: openssl rand -hex 32
CRON_SECRET=tu_secret_aqui

# ============================================
# CONFIGURACIÓN OPCIONAL
# ============================================

# Credenciales del usuario administrador del panel
# Si no se especifican, se usarán los valores por defecto
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# URL base del panel (para links y redirecciones)
BASE_URL=http://localhost:44444

# Puerto donde correrá el servidor (default: 44444)
PORT=44444

# Zona horaria (default: Etc/UTC)
# Ejemplos: America/Mexico_City, America/New_York, Europe/Madrid
TZ=Etc/UTC

# Entorno de ejecución (production o development)
NODE_ENV=production

# Deshabilitar telemetría de Next.js
NEXT_TELEMETRY_DISABLED=1
